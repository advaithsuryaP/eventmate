import { environment } from '../../../environments/environment';

export const API_URL_MAP = {
    EVENT: `${environment.API_URL}/event`,
    EVENTS: `${environment.API_URL}/events`,
    DOMAINS: `${environment.API_URL}/domains`
};
